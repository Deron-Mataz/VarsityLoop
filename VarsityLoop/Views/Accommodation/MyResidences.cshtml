@model List<VarsityLoop.Models.Entities.Accommodation>
@{
    ViewData["Title"] = "My Residences";
}

<div class="container py-5">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">@TempData["SuccessMessage"]</div>
    }

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">My Residences</h1>
        <a asp-controller="Accommodation" asp-action="Create" class="btn btn-primary">List a Residence</a>
    </div>

    @if (Model.Count == 0)
    {
        <div class="vl-card vl-empty-state">
            <h5>No residences listed yet</h5>
            <p class="mb-0">Create your first listing to start renting out rooms.</p>
        </div>
    }
    else
    {
        <div class="vl-card">
            <table class="table mb-0 align-middle">
                <thead>
                    <tr>
                        <th>Residence</th>
                        <th>Rent</th>
                        <th>Status</th>
                        <th>Views</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var accommodation in Model)
                    {
                        <tr>
                            <td>
                                <a asp-controller="Accommodation" asp-action="Details" asp-route-id="@accommodation.Id" class="text-reset text-decoration-none fw-semibold">@accommodation.ResidenceName</a>
                            </td>
                            <td>R@(accommodation.MonthlyRent.ToString("N2"))</td>
                            <td>
                                @if (accommodation.Status == nameof(VarsityLoop.Models.Entities.AccommodationStatus.Active))
                                {
                                    <span class="badge badge-green">Active</span>
                                }
                                else if (accommodation.Status == nameof(VarsityLoop.Models.Entities.AccommodationStatus.Paused))
                                {
                                    <span class="badge bg-secondary">Paused</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">@accommodation.Status</span>
                                }
                            </td>
                            <td>@accommodation.Views</td>
                            <td class="text-end">
                                <div class="d-flex gap-2 justify-content-end">
                                    <a asp-controller="Accommodation" asp-action="Edit" asp-route-id="@accommodation.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                                    @if (accommodation.Status == nameof(VarsityLoop.Models.Entities.AccommodationStatus.Active))
                                    {
                                        <form asp-controller="Accommodation" asp-action="Pause" asp-route-id="@accommodation.Id" method="post">
                                            <button type="submit" class="btn btn-sm btn-outline-secondary">Pause</button>
                                        </form>
                                    }
                                    else if (accommodation.Status == nameof(VarsityLoop.Models.Entities.AccommodationStatus.Paused))
                                    {
                                        <form asp-controller="Accommodation" asp-action="Reactivate" asp-route-id="@accommodation.Id" method="post">
                                            <button type="submit" class="btn btn-sm btn-outline-primary">Reactivate</button>
                                        </form>
                                    }
                                    <form asp-controller="Accommodation" asp-action="Delete" asp-route-id="@accommodation.Id" method="post" onsubmit="return confirm('Delete this residence?');">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
