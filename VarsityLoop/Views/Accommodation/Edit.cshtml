@model VarsityLoop.Models.ViewModels.Accommodation.AccommodationFormViewModel
@{
    ViewData["Title"] = "Edit Residence";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <h1 class="h3 mb-4">Edit Residence</h1>

            <form asp-controller="Accommodation" asp-action="Edit" method="post" enctype="multipart/form-data" novalidate>
                <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                <partial name="_AccommodationFormFields" model="Model" />
                <button type="submit" class="btn btn-primary">Save Changes</button>
                <a asp-controller="Accommodation" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>
